import { css } from 'styled-components'

export const contactStyle = css`
  .contact {
    form {
      text-align: center;
    }
    &__mail {
      margin-bottom: 0.5em;
      text-align: center;
    }
  }
`